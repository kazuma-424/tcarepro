  <div class="beige-wrapper">
  
            <div class="heading"><h2>パスワードをお忘れの場合?</h2></div>


<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

<table class="worker_table">
        			<colgroup>
            			<col width="40%">
            			<col width="60%">
       			　 </colgroup>
        		<tr>
					<th>登録しているメールアドレス<span class="label label-blue">必須</span></br>
									<small>aaa@crold-valieat.jp</small></th>
					<td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
				</tr>		
			        <th colspan="2"><%= f.submit "パスワードを再送する" %></th>
				</tr>		
</table>

<% end %>

<%= render "devise/shared/links" %>
