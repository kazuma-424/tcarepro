  <div class="beige-wrapper">

            <div class="heading"><h2>パスワードを忘れた場合</h2>
            <p>Forgot your password?</p></div>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

			<table>
        			<colgroup>
            			<col width="40%">
            			<col width="60%">
       			　 </colgroup>
        		<tr>
					<th>メールアドレス<span class="label label-blue">必須</span></br>
									<small>aaa@crold-valieat.jp</small></th>
					<td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
				</tr>
        					<tr>
									<th colspan="2"><%= f.submit "パスワードを再発行しますか？"%></th>
				</tr>
</table>
<% end %>
<br>
<div class="center">
  <%= render "admins/shared/links" %>
</div>
