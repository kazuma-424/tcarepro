%table{width: "90%", class:"tablesorter", id:"company-index-table"}
  %thead
    %tr
      %th
      %th 会社名
      %th 進捗
      %th 申込者
      %th 入社日
      %th 転換日
      %th 支給申請開始日
  %tbody
    - comments.each do |comment|
      %tr
        %td= check_box 'company', 'check', {:id => "#{comment.company.id}", :class=>"company-checkbox", :data=>{:email=>"#{comment.company.e_mail}", :comment_id=>"#{comment.id}"}}, true, false
        %td= link_to comment.company.company, company_path(comment.company)
        %td= comment.carriaup_progress
        %td= comment.carriaup_each_name
        %td= comment.carriaup_each_limited_start
        %td= comment.carriaup_each_regular
        %td= comment.carriaup_each_supply
.companies-pagenate
  = paginate comments

/ <div class="modal fade" id="company-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true"></div
